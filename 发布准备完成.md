# bb-browser-api 发布准备完成 ✅

## 📦 项目信息

- **包名称**: `bb-browser-api`
- **当前版本**: `0.11.5`
- **全局安装**: `npm install -g bb-browser-api`
- **GitHub**: https://github.com/skVPN/bb-browser-api
- **npm**: https://www.npmjs.com/package/bb-browser-api (待发布)

## ✅ 已完成的配置

### 1. 包配置更新

- ✅ 包名称从 `bb-browser` 改为 `bb-browser-api`
- ✅ 可执行命令更新为 `bb-browser-api`、`bb-browser-api-mcp`、`bb-browser-api-provider`
- ✅ 仓库信息更新为 `skVPN/bb-browser-api`
- ✅ 添加发布相关的 npm scripts

### 2. 发布脚本

创建了以下自动化脚本：

- ✅ `scripts/publish.sh` - Linux/Mac 发布脚本
- ✅ `scripts/publish.ps1` - Windows PowerShell 发布脚本
- ✅ `scripts/pre-publish-check.js` - 发布前检查脚本

### 3. 发布文档

创建了完整的发布文档：

- ✅ `docs/NPM_PUBLISH.md` - 详细发布指南（完整步骤）
- ✅ `PUBLISH_README.md` - 快速发布说明（简明版）
- ✅ `docs/PUBLISH_SUMMARY.md` - 发布配置总结
- ✅ `PUBLISH_CHECKLIST.md` - 发布检查清单
- ✅ `发布准备完成.md` - 本文件

### 4. 配置文件

- ✅ `.npmignore` - 控制发布时包含的文件
- ✅ `package.json` - 更新包信息和脚本
- ✅ `README.md` - 更新命令示例

### 5. 发布前检查

运行检查脚本验证：

```bash
node scripts/pre-publish-check.js
```

**检查结果**: ✅ 所有检查通过，可以发布

## 🚀 快速发布

### Windows 用户

```powershell
# 修复版本 (0.11.5 -> 0.11.6)
.\scripts\publish.ps1 patch

# 次版本 (0.11.5 -> 0.12.0)
.\scripts\publish.ps1 minor

# 主版本 (0.11.5 -> 1.0.0)
.\scripts\publish.ps1 major
```

### Linux/Mac 用户

```bash
# 修复版本
./scripts/publish.sh patch

# 次版本
./scripts/publish.sh minor

# 主版本
./scripts/publish.sh major
```

## 📋 首次发布步骤

### 1. 登录 npm

```bash
npm login
```

### 2. 验证登录

```bash
npm whoami
```

### 3. 检查包名可用性

```bash
npm view bb-browser-api
```

如果返回 404，说明包名可用。

### 4. 运行发布前检查

```bash
node scripts/pre-publish-check.js
```

### 5. 本地测试

```bash
# 打包
npm pack

# 安装测试
npm install -g ./bb-browser-api-0.11.5.tgz

# 测试命令
bb-browser-api --version
bb-browser-api --help
```

### 6. 发布到 npm

#### 使用脚本（推荐）

```powershell
# Windows
.\scripts\publish.ps1 patch
```

```bash
# Linux/Mac
./scripts/publish.sh patch
```

#### 手动发布

```bash
npm publish --access public
```

### 7. 推送到 GitHub

```bash
git push
git push --tags
```

### 8. 创建 GitHub Release

在 GitHub 仓库页面创建新的 Release，关联刚才推送的 tag。

## 📚 文档结构

```
bb-browser-api/
├── PUBLISH_README.md              # 快速发布说明
├── PUBLISH_CHECKLIST.md           # 发布检查清单
├── 发布准备完成.md                 # 本文件
├── docs/
│   ├── NPM_PUBLISH.md             # 完整发布指南
│   └── PUBLISH_SUMMARY.md         # 发布配置总结
└── scripts/
    ├── publish.sh                 # Linux/Mac 发布脚本
    ├── publish.ps1                # Windows 发布脚本
    └── pre-publish-check.js       # 发布前检查脚本
```

## 🔍 发布脚本功能

### publish.sh / publish.ps1

自动化发布流程，包括：

1. ✅ 检查 npm 登录状态
2. ✅ 检查工作区状态
3. ✅ 安装依赖
4. ✅ 运行构建
5. ✅ 运行测试（可选）
6. ✅ 更新版本号
7. ✅ 本地打包测试
8. ✅ 发布到 npm
9. ✅ 创建 git commit 和 tag
10. ✅ 提示后续步骤

### pre-publish-check.js

发布前检查脚本，验证：

- ✅ package.json 配置完整性
- ✅ 构建产物存在性
- ✅ shebang 正确性
- ✅ 必需文件存在性
- ✅ .npmignore 配置合理性
- ✅ 依赖配置正确性
- ✅ Node.js 版本要求

## 📦 发布后验证

### 1. 检查 npm 包页面

访问: https://www.npmjs.com/package/bb-browser-api

确认：
- ✅ 版本号正确
- ✅ README 显示正常
- ✅ 文件列表正确

### 2. 测试全局安装

```bash
# 卸载本地版本
npm uninstall -g bb-browser-api

# 从 npm 安装
npm install -g bb-browser-api

# 验证
bb-browser-api --version
bb-browser-api daemon start
```

### 3. 测试 MCP 集成

在 Claude Desktop 或 Cursor 的 MCP 配置中添加：

```json
{
  "mcpServers": {
    "bb-browser-api": {
      "command": "npx",
      "args": ["-y", "bb-browser-api", "--mcp"]
    }
  }
}
```

## 🎯 版本管理

### 语义化版本规则

- **MAJOR** (1.0.0): 不兼容的 API 变更
- **MINOR** (0.1.0): 向后兼容的功能新增
- **PATCH** (0.0.1): 向后兼容的问题修复

### 版本更新示例

| 变更类型 | 当前版本 | 新版本 | 命令 |
|---------|---------|--------|------|
| Bug 修复 | 0.11.5 | 0.11.6 | `npm version patch` |
| 新功能 | 0.11.5 | 0.12.0 | `npm version minor` |
| 重大变更 | 0.11.5 | 1.0.0 | `npm version major` |

## ⚠️ 注意事项

### 发布前

1. 确保所有代码已提交并推送到 GitHub
2. 确保所有测试通过
3. 确保构建无错误
4. 运行发布前检查脚本

### 发布时

1. 使用正确的版本号类型（patch/minor/major）
2. 确认发布前的提示信息
3. 如果启用了 2FA，准备好验证码

### 发布后

1. 验证 npm 包页面
2. 测试全局安装
3. 推送代码和标签到 GitHub
4. 创建 GitHub Release
5. 更新相关文档

## 🆘 常见问题

### Q: 发布失败，提示需要 2FA

```bash
npm publish --otp=123456
```

### Q: 包名已被占用

使用 scoped package：

```json
{
  "name": "@your-username/bb-browser-api"
}
```

### Q: 命令安装后找不到

检查：
1. `package.json` 中的 `bin` 字段是否正确
2. 构建产物是否包含 shebang
3. 文件权限是否正确（Linux/Mac）

### Q: 如何撤销发布

```bash
# 72 小时内可以撤销
npm unpublish bb-browser-api@0.11.5
```

**注意**: 撤销后的包名和版本号 24 小时内无法重新发布。

## 📖 相关文档

- [完整发布指南](docs/NPM_PUBLISH.md) - 详细的发布步骤和说明
- [快速发布说明](PUBLISH_README.md) - 简明的发布指南
- [发布配置总结](docs/PUBLISH_SUMMARY.md) - 配置变更总结
- [发布检查清单](PUBLISH_CHECKLIST.md) - 发布前后的检查清单

## 🔗 相关链接

- GitHub 仓库: https://github.com/skVPN/bb-browser-api
- npm 包页面: https://www.npmjs.com/package/bb-browser-api (待发布)
- 原始项目: https://github.com/epiral/bb-browser
- npm 文档: https://docs.npmjs.com/
- 语义化版本: https://semver.org/lang/zh-CN/

## 💬 支持

如有问题，请在 GitHub 提交 Issue：
https://github.com/skVPN/bb-browser-api/issues

---

## ✨ 下一步

现在你可以：

1. **首次发布**: 按照上面的步骤进行首次发布
2. **查看文档**: 阅读详细的发布文档了解更多信息
3. **测试脚本**: 运行发布脚本进行自动化发布

祝发布顺利！🎉
